import * as React from 'react';
import ISendEmailProps  from './ISendEmailProps';

export default function ReadStatus (props: ISendEmailProps){

    return(
        <>
            Test send email
        </>
    );
}
